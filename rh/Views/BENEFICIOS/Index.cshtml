@model IEnumerable<rh.Data.BENEFICIOS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BENDESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BENDESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_BENEFICIO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_BENEFICIO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_BENEFICIO })
        </td>
    </tr>
}

</table>

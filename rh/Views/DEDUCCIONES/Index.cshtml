@model IEnumerable<rh.Data.DEDUCCIONES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DEDDESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DEDDESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_DEDUCCION }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_DEDUCCION }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_DEDUCCION })
        </td>
    </tr>
}

</table>

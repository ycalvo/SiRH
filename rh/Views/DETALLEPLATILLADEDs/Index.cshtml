@model IEnumerable<rh.Data.DETALLEPLATILLADED>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MONTO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DETALLEPLATILLADED1.ID_PLANILLA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DETALLEPLATILLADED2.ID_PLANILLA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MONTO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DETALLEPLATILLADED1.ID_PLANILLA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DETALLEPLATILLADED2.ID_PLANILLA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>

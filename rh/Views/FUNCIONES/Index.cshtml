@model IEnumerable<rh.Data.FUNCIONES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FUNCDESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FUNCDESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_FUNCIONES }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_FUNCIONES }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_FUNCIONES })
        </td>
    </tr>
}

</table>

@model IEnumerable<rh.Data.TBFUNCIONARIO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FUNNOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FUNAPELLIDO1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FUNAPELLIDO2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FUNNOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FUNAPELLIDO1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FUNAPELLIDO2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_FUNCIONARIO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_FUNCIONARIO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_FUNCIONARIO })
        </td>
    </tr>
}

</table>
